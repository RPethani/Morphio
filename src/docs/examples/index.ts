/**
 * @packageDocumentation
 * @module Examples
 */

/**
 * # Morphio Examples
 *
 * This section contains comprehensive examples demonstrating various features of Morphio.
 * Each example is taken from our test cases and shows real-world usage patterns.
 *
 * ## Categories
 *
 * 1. [Basic Types](./basic-types.html)
 *    - String, Number, Boolean, Date
 *    - Required vs Optional properties
 *    - Type inference
 *
 * 2. [Containers](./containers.html)
 *    - Arrays of primitive types
 *    - Arrays of objects
 *    - Maps with primitive values
 *    - Maps with complex values
 *    - Nested containers
 *
 * 3. [Inline Objects](./inline-objects.html)
 *    - Simple inline objects
 *    - Nested structures
 *    - Inline objects in containers
 *    - Optional properties
 *
 * 4. [Inheritance](./inheritance.html)
 *    - Basic class inheritance
 *    - Multi-level inheritance
 *    - Interface inheritance
 *    - Property overrides
 *
 * ## Example Structure
 *
 * Each example follows this structure:
 * 1. Type/class definition with decorators or schema
 * 2. Sample data creation
 * 3. Serialization example
 * 4. Deserialization example
 * 5. Expected results
 *
 * ## Running the Examples
 *
 * All examples are extracted from our test suite and can be run directly.
 * To try them:
 *
 * 1. Install Morphio:
 * ```bash
 * npm install morphio
 * ```
 *
 * 2. Copy any example into a TypeScript file
 *
 * 3. Make sure your tsconfig.json has the necessary options:
 * ```json
 * {
 *   "compilerOptions": {
 *     "experimentalDecorators": true,
 *     "emitDecoratorMetadata": true
 *   }
 * }
 * ```
 *
 * 4. Run the example:
 * ```bash
 * ts-node your-example.ts
 * ```
 */
export const examples = 'This file contains documentation examples.';
